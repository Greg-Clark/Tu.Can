import React from 'react';

class Messaging extends React.Component {
    render() {
        return(
            <div>
                Messaging page
            </div>
        );
    }
}

export default Messaging;
